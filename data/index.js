const { bio } = require("./bio");
const { users } = require("./users");
const { blogsAll } = require("./blogsAll");
const { expLists } = require("./experiences");
const { avatarPhoto, portPhoto } = require("./images");

module.exports = {
  bio,
  users,
  blogsAll,
  expLists,
  avatarPhoto,
  portPhoto,
};
